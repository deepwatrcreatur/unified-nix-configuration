{
  system.defaults.CustomSystemPreferences."com.apple.menuextra.clock" = {
    # Show 24-hour time
    Show24Hour = true;
    # Show date in menu bar
    ShowDate = true;
    # Show day of week
    ShowDayOfWeek = true;
    # Don't use analog clock
    IsAnalog = false;
  };
}
