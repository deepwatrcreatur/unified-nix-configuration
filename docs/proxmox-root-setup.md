# Proxmox Root Setup Moved

The operational Proxmox host setup guide has moved to the `proxmox-host-configuration` repo.

Current location:

- `/home/deepwatrcreatur/flakes/proxmox-host-configuration/docs/proxmox-root-setup.md`

`unified-nix-configuration` still owns the `proxmox-root` Home Manager output itself. The runbook for deploying and operating it now lives with the rest of the Proxmox host operations material.
